# 📄 合约异动提醒机器人 v1.0 需求文档

## 1. 产品概述

合约异动提醒机器人是一款面向合约交易用户的轻量级工具。它通过实时监听交易所行情，识别出符合条件的异动（价格、成交量、资金费率等），并将提醒推送到用户的
**Telegram**，帮助用户及时发现交易机会，降低盯盘压力。

**定位**：辅助交易工具，不提供直接交易功能。\
**核心价值**：\
- 帮助用户发现市场异动机会。\
- 提供即时提醒，减少错过行情。\
- 降低手动盯盘成本。

------------------------------------------------------------------------

## 2. 用户群体

-   散户合约玩家（喜欢短线/日内交易）。\
-   小资金量套利者（需要资金费率提醒）。\
-   不方便时刻盯盘的兼职交易者。

------------------------------------------------------------------------

## 3. 功能清单 (MVP 范围)

### 3.1 监控功能

1.  **价格异动检测**
    -   条件：在指定时间窗口（默认5分钟）内，涨跌幅 ≥ X%（默认5%）。\
    -   示例：BTCUSDT 在5分钟内上涨 6%，触发提醒。
2.  **成交量异动检测**
    -   条件：当前成交量相比过去 N 个周期均值放大 ≥ Y 倍（默认3倍）。\
    -   示例：ETHUSDT 5分钟成交量是均值的 4 倍。
3.  **资金费率监控（可选，若时间允许）**
    -   条件：资金费率 ≥ +0.1% 或 ≤ -0.1%。\
    -   示例：BNBUSDT 永续合约资金费率 = 0.12%，触发提醒。

------------------------------------------------------------------------

### 3.2 推送功能

1.  **Telegram Bot 推送**
    -   推送内容：

        -   交易对\
        -   触发类型（价格 / 成交量 / 资金费率）\
        -   具体数值（涨跌幅、倍数、资金费率）\
        -   时间戳\

    -   格式示例：

            🚨 异动提醒
            币种：BTCUSDT
            类型：价格异动
            涨幅：+5.8%（5分钟）
            时间：2025-09-02 18:35
2.  **推送频率限制**
    -   避免重复轰炸：对同一交易对的相同类型异动，每 10
        分钟最多推送一次。

------------------------------------------------------------------------

### 3.3 配置功能

1.  **用户配置（v1 简化版）**
    -   在配置文件 / 数据库中设置：
        -   监控交易对（默认全市场前100大交易对）。\
        -   涨跌幅阈值（默认 5%）。\
        -   成交量倍数阈值（默认 3x）。
2.  **全局配置**
    -   WebSocket 数据源（Binance `!ticker@arr`）。\
    -   推送目标（Telegram chat_id）。

------------------------------------------------------------------------

### 3.4 系统功能

1.  **后台运行**
    -   能在服务器上 7x24 小时运行。\
    -   自动重连交易所 WebSocket。
2.  **日志记录**
    -   记录每一次触发事件（存 SQLite）。\
    -   包含：交易对、类型、数值、时间、推送状态。

------------------------------------------------------------------------

## 4. 技术方案

### 4.1 架构

    [Binance WS] → [数据接入模块] → [事件检测引擎] → [推送模块] → [Telegram用户]
                                              ↓
                                          [SQLite日志]

### 4.2 技术栈

-   **语言**：TypeScript / Node.js\
-   **数据库**：SQLite（轻量，便于快速实现日志与配置）\
-   **推送**：Telegram Bot API\
-   **部署**：Docker 容器 / VPS

------------------------------------------------------------------------

## 5. 非功能需求

1.  **性能**：可稳定处理 Binance 全市场 ticker 数据流（上千交易对）。\
2.  **健壮性**：断线自动重连，推送失败自动重试。\
3.  **易扩展**：未来支持 OKX、Bybit。

------------------------------------------------------------------------

## 6. MVP 范围缩减版（保证1个月完成）

-   仅支持 Binance。\
-   功能只做 **价格异动提醒**（5分钟涨跌幅 \> X%）。\
-   推送只支持 **Telegram**。\
-   配置写死在 `.env` 文件里（阈值、chat_id）。\
-   日志只用 SQLite 记录事件。

------------------------------------------------------------------------

## 7. 后续迭代方向

-   v1.1：成交量异动提醒。\
-   v1.2：资金费率提醒。\
-   v1.3：用户 Web Dashboard（可配置阈值、查看历史记录）。\
-   v2.0：支持多交易所 + 策略信号提醒。
